<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:defaultValue="180"
        android:entries="@array/pref_sync_frequency_titles"
        android:entryValues="@array/pref_sync_frequency_values"
        android:key="data_sync_worker_tag"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_sync_frequency" />
    <ListPreference
        android:defaultValue="15"
        android:entries="@array/pref_launch_notification_titles"
        android:entryValues="@array/pref_launch_notification_values"
        android:key="notification_launch_worker_tag"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_launch_notification" />
</PreferenceScreen>