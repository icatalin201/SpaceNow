<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/main_fragment">

    <fragment
        android:id="@+id/news_fragment"
        android:name="space.pal.sig.old.view.news.NewsFragment"
        tools:layout="@layout/fragment_news" />

    <fragment
        android:id="@+id/launches_fragment"
        android:name="space.pal.sig.old.view.launches.LaunchesFragment"
        tools:layout="@layout/fragment_launches" />

    <fragment
        android:id="@+id/main_fragment"
        android:name="space.pal.sig.view.main.MainFragment"
        tools:layout="@layout/fragment_main" />

</navigation>